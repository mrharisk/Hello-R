# This is from Github
# Hello from Github
# Today is my first lesson

# Data Types and Basic Operation(Part 1)
a=5
class(a)
a=5L
class(a)
# L is used to show the number that it is integer
# and L is always used in a right place of number
# vector is a most basic object
# which contains same classes of objects
# but list is a exception which can use different classes of objects
# Empty vector can be created with the vector() function
